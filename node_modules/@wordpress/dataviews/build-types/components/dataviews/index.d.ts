/**
 * External dependencies
 */
import type { ReactNode } from 'react';
import type { Action, Field, View, SupportedLayouts } from '../../types';
type ItemWithId = {
    id: string;
};
type DataViewsProps<Item> = {
    view: View;
    onChangeView: (view: View) => void;
    fields: Field<Item>[];
    search?: boolean;
    searchLabel?: string;
    actions?: Action<Item>[];
    data: Item[];
    isLoading?: boolean;
    paginationInfo: {
        totalItems: number;
        totalPages: number;
    };
    defaultLayouts: SupportedLayouts;
    selection?: string[];
    onChangeSelection?: (items: string[]) => void;
    onClickItem?: (item: Item) => void;
    isItemClickable?: (item: Item) => boolean;
    header?: ReactNode;
    getItemLevel?: (item: Item) => number;
} & (Item extends ItemWithId ? {
    getItemId?: (item: Item) => string;
} : {
    getItemId: (item: Item) => string;
});
export default function DataViews<Item>({ view, onChangeView, fields, search, searchLabel, actions, data, getItemId, getItemLevel, isLoading, paginationInfo, defaultLayouts, selection: selectionProperty, onChangeSelection, onClickItem, isItemClickable, header, }: DataViewsProps<Item>): import("react").JSX.Element;
export {};
//# sourceMappingURL=index.d.ts.map